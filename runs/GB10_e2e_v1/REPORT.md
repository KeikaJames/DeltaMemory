# GB10 End-to-End Deployment Witness — v1

**Status**: ⏳ Pending execution on spark1 (GB10).

This report is generated by `scripts/deploy_gb10_e2e.sh`.  Run it on spark1
to populate the actual benchmark numbers:

```bash
cd /home/gabira/projects/RCV-HC
source .venv-gb10/bin/activate
bash scripts/deploy_gb10_e2e.sh
```

---

## What this measures

| Metric | Target |
|---|---|
| Recall@5 | ≥ 0.80 (80% of facts retrievable in top-5) |
| Latency p50 | < 2000 ms |
| Latency p95 | < 5000 ms |
| Peak RSS | < 90 GB (within GB10 VRAM budget) |

---

## Setup

- Model: `gemma-4-31B-it` at `/home/gabira/Desktop/workspace/models/whitelist/gemma-4-31B-it`
- Bank: 100 synthetic facts (`Entity{i:04d} → Value{i:04d}`)
- Requests: 100 fact-recall probes via POST /generate
- Server: `deltamemory.api.app:app` (uvicorn, port 8741)
- Device: CUDA / bfloat16 / alpha=1.0

---

*Results will be written here automatically when the script completes.*
