# Apple Silicon nightly environment for testing latest PyTorch MPS fixes.
#
# Use this only when stable PyTorch cannot initialize MPS on a newer macOS.
# The project does not depend on torchvision/torchaudio, but they are included
# to keep the common PyTorch stack aligned where nightly wheels exist.
--extra-index-url https://download.pytorch.org/whl/nightly/cpu
--pre

torch
torchvision

transformers==5.7.0
accelerate==1.13.0
safetensors==0.7.0
tokenizers==0.22.2
pytest==9.0.3
